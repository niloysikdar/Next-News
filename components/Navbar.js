import styles from '../styles/Navbar.module.css';

const Navbar = () => {
  return (
    <div className={styles.navbar}>
      <h2 className={styles.title}>News App using Next.js</h2>
    </div>
  );
};

export default Navbar;
